{% extends model('template') %}

{% define data = {
    form: form
} %}

{% block body %}
    <div class="payment-method">
        <p>{{ 'novalnet.barzahlen-payment-description' | trans }}</p>        
        {{ form_errors(data.form.novalnetBarzahlen) }}
        {{ form_widget(data.form.novalnetBarzahlen) }}
    </div>
{% endblock %}
