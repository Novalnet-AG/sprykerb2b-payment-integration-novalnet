{% extends model('template') %}

{% define data = {
    form: form
} %}

{% block body %}
    <div class="payment-method">
        <p>{{ 'novalnet.paypal-payment-description' | trans }}</p>
        {{ form_errors(data.form.novalnetPaypal) }}
        {{ form_widget(data.form.novalnetPaypal) }}
    </div>
{% endblock %}
