{% extends model('template') %}

{% define data = {
    form: form
} %}

{% block body %}
    <div class="payment-method">
        <p>{{ 'novalnet.multibanco-payment-description' | trans }}</p>        
        {{ form_errors(data.form.novalnetMultibanco) }}
        {{ form_widget(data.form.novalnetMultibanco) }}
    </div>
{% endblock %}
