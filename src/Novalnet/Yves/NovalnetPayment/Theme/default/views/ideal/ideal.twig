{% extends model('template') %}

{% define data = {
    form: form
} %}

{% block body %}
    <div class="payment-method">
        <p>{{ 'novalnet.ideal-payment-description' | trans }}</p>
        {{ form_errors(data.form.novalnetIdeal) }}
        {{ form_widget(data.form.novalnetIdeal) }}
    </div>
{% endblock %}
