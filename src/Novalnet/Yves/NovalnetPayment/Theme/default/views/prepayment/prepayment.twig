{% extends model('template') %}

{% define data = {
    form: form
} %}

{% block body %}
    <div class="payment-method">
        <p>{{ 'novalnet.invoice-payments-description' | trans }}</p>        
        {{ form_errors(data.form.novalnetPrepayment) }}
        {{ form_widget(data.form.novalnetPrepayment) }}
    </div>
{% endblock %}
